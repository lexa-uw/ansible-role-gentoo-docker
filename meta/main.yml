galaxy_info:
  author: Lexa
  description: Role for adding docker/docker-compose/docker-registry/docker-swarm.
  company: Freelance

  issue_tracker_url: https://github.com/lexa-uw/ansible-role-gentoo.docker/issues

  license: BSD

  min_ansible_version: 1.2

  platforms:
  - name: Gentoo
    versions:
      - all

  galaxy_tags: [ "docker", "compose", "registry", "swarm", "gentoo"]

dependencies: [
  { role: lexa-uw.gentoo-package-meta, packages: "{{ docker_packages_meta }}" }
]
